using System;
using System.Linq;

namespace _04._Array_Rotation
{
    class Program
    {
        static void Main(string[] args)
        //{
        //    string[] array = Console.ReadLine().Split();//Creating an array 
        //    int rotations = int.Parse(Console.ReadLine());//Asking for how many rotations we want

            double  chisloto = Math.PI;
        Console.WriteLine(chisl);


            //for (int i = 0; i < rotations; i++)//Making a for loop until the chosed rotations
            //{
            //    string firstIndex = array[0];//Saving current first index in the array

            //    for (int j = 0; j < array.Length-1; j++)//Making a for loop until the lengh of the array - 1(because we are moving 1 index ahead and to avoid error)
            //    {
            //        array[j] = array[j + 1];//save each index with the next index value
            //    }
            //    array[array.Length - 1] = firstIndex;//Replase the last index value with the first index value that we saved earlier 
            //}

            //foreach (var item in array)//Make a foreach loop to access each index in the array
            //{
            //    Console.Write($"{item} ");//print each index as finish rotated product
            //}

            


        }
    }
}
